---

# The login username/password for the USB provisioner.
provisioner_login_username: prover
provisioner_login_password: changeme123

# The login username/password for the target system.
target_login_username: tony
target_login_password: changeme123

# *Optional. Set only if you want the provisioner and/or
# the target system to connect to a WiFi network. Be advised
# that these credentials will be stored in plain text on the
# USB provisioner, in the netplan config file and in this file,
# which gets copied to the /prov folder.
wifi_ssid:
wifi_password:
